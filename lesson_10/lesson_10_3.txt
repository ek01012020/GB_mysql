1. Индексы итак есть, т.к. используем внешние ключи
2. Длинных полей нет, т.к. медиа содержит только имена файлов
3. Можно было бы избавиться от target_types_id и media_types_id, используя 
	ENUM, но мы их все равно не используем в запросе
4. Количество JOIN не критично

Поэтому наиболее слабое место это агрегатные функции COUNT().
Можно добавить столбец activity  в profiles и увеличивать значение при каждой новой записи
в таблицы likes, media, messages. ИЛИ три разных COUNT(), если это требуется в других
запросах часто. Это наверное оправданно, если у нас используется рейтинг наиболее активных пользователей.
И поэтому требуется постоянный контроль активности.